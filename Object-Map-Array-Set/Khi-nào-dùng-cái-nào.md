<!-- Khi nào dùng cái nào?

Object → cấu trúc cơ bản, dùng khi key là string đơn giản.

Array → dữ liệu có thứ tự, cần duyệt nhiều.

Map → cần lưu cặp key/value, key có thể là bất kỳ kiểu nào.

Set → cần lưu giá trị duy nhất, tránh trùng lặp. -->    