// Array là danh sách các phần tử theo thứ tự.
// Truy cập bằng chỉ số (index).

// Ví dụ:

let arr = [10, 20, 30];
console.log(arr[1]);
arr.push(40);
console.log(arr); // thêm vào cuối



// 👉 Ưu điểm:

// Lưu dữ liệu có thứ tự.

// Có nhiều phương thức mạnh mẽ (map, filter, reduce).

// 👉 Nhược điểm:

// Tìm kiếm phần tử mất O(n) nếu không biết index.