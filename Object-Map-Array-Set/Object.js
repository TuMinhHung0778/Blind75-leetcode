// Là cấu trúc dữ liệu cơ bản trong JS.

// Lưu dữ liệu dưới dạng key → value.

// Key thường là string hoặc symbol (mặc định tự ép thành string).

// Ví dụ:

let obj = { name: "Hung", age: 23, id: 9135 };

console.log(obj.name);
console.log(obj.age);
console.log(obj.id);


// 👉 Ưu điểm:

// Đơn giản, dễ dùng.

// Phù hợp khi key chắc chắn là string.

// 👉 Nhược điểm:

// Không dễ lặp qua các key theo thứ tự thêm vào.

// Key không thể là object, array, hay bất kỳ kiểu phức tạp nào.
